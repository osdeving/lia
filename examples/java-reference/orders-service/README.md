# Orders Service Reference

Projeto Java manual usado como referencia na demo comparativa.

- Linguagem: Java 21
- Estilo: arquitetura em camadas leves com `domain`, `port`, `application`, `infrastructure` e `bootstrap`
- Compilacao local: `javac $(find src/main/java -name '*.java')`
